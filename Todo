Completed:
- CI/CD pipeline (`.github/workflows/ci.yml`) — builds & pushes Docker images on push to main
- Generate Timetable feature (Course Module):
  - Backend: TimetableEntry entity, repository, DTOs, service, controller
  - Frontend: Admin timetable management (CRUD + auto-generate), student/lecturer weekly view
  - Seed data with 12 timetable entries across all departments
- Forgot Password / Reset Password pages (frontend + backend)
- Lecturer "Students" view (frontend + backend)
- Groq API integration (via OpenAI-compatible endpoint):
  - Switched from `spring-ai-google-genai` to `spring-ai-openai` (Spring AI 1.1.8)
  - Uses Groq's OpenAI-compatible API at `api.groq.com/openai` with Llama 3.3 70B
  - Configurable provider selection via `spring.ai.model.chat=openai`
  - AiChatConfig creates ChatClient bean for Groq
  - Groq config with GROQ_API_KEY env var support
  - Custom TF-IDF EmbeddingModel for RAG (Groq lacks embedding endpoints)
  - docker-compose updated to pass GROQ_API_KEY

Remaining items you may want:
- RAG System (Bonus feature per requirements doc)
- Cloud deployment (AWS/Azure per requirements doc)
